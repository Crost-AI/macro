[workspace]
members = ["tools/inspect-wasm"]
exclude = [".turso-source"]
resolver = "2"

[workspace.package]
edition = "2021"
rust-version = "1.94"
license = "UNLICENSED"

[package]
name = "turso-opfs-spike"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = ["native-test"]
wasm-minimum = ["turso_core/fs", "turso_core/uuid"]
native-test = ["wasm-minimum", "turso_core/pure-rust-crypto"]

[dependencies]
js-sys = "=0.3.98"
# `scripts/materialize-turso.sh` exports the exact, clean fork revision here.
# The committed dependency is relative; the supplied fork is never modified.
turso_core = { path = ".turso-source/core", default-features = false }
wasm-bindgen = "=0.2.121"
wasm-bindgen-futures = "=0.4.71"

[dependencies.web-sys]
version = "=0.3.98"
features = [
  "DedicatedWorkerGlobalScope",
  "DomException",
  "File",
  "FileSystemDirectoryHandle",
  "FileSystemFileHandle",
  "FileSystemGetDirectoryOptions",
  "FileSystemGetFileOptions",
  "FileSystemReadWriteOptions",
  "FileSystemSyncAccessHandle",
  "StorageManager",
  "WorkerGlobalScope",
  "WorkerNavigator",
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "debuginfo"
